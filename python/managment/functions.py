
class Managment_functions:

    def read_files(self, paths=''):
        # print(type(paths))
        with open(paths, 'r') as path_files:
            return type(path_files)
            """ for files in path_files:
                print(files) """
